/* Host diagnostics probe (Diagram Studio). A script the diagram view asks the host to load from a2uicatalog.ai, to find out whether the
 * host lets a view run scripts from a declared resource domain (resourceDomains). It does nothing else. */
window.__a2uiProbeScript = "ran";
