{
  "name": "@a2ui/mcp",
  "version": "0.1.0",
  "description": "Local MCP server for the A2UI Atomic Catalog: browse the 474-atom vocabulary, validate a payload (hallucinated atom = parse error, not a broken screen), get a shareable preview URL, and deploy a training.md to your own Google Apps Script. Pure-local for browsing/validation — no network, no secret, no account.",
  "type": "module",
  "bin": { "a2ui-mcp": "server.mjs" },
  "engines": { "node": ">=18" },
  "license": "MIT",
  "author": "Curtis Krygier",
  "homepage": "https://a2uicatalog.ai",
  "repository": { "type": "git", "url": "git+https://github.com/a2uicatalog/a2ui.git", "directory": "mcp" },
  "bugs": "https://github.com/a2uicatalog/a2ui/issues",
  "keywords": ["mcp", "model-context-protocol", "a2ui", "ui-generation", "claude", "agent", "google-apps-script"],
  "files": ["server.mjs", "demo.mjs", "test.mjs", "scripts", "data", "README.md"],
  "scripts": {
    "test": "node test.mjs",
    "demo": "node demo.mjs",
    "sync-data": "node scripts/sync-data.mjs",
    "prepublishOnly": "npm run sync-data && npm test"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.0"
  }
}
