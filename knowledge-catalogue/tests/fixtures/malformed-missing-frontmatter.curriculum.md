## No Frontmatter At All {#concept}
This file has no --- delimited YAML frontmatter block, which must raise the
same error signal in both the Python reference and the JS twin — used as
the parity harness's error-path fixture.
