{
  "title": "Atoms, catalogs & graduation",
  "theme": "dark",
  "blocks": [
    {
      "type": "breadcrumb",
      "items": [
        {
          "label": "Learn A2UI + MCP",
          "url": "/s/learn-a2ui"
        },
        {
          "label": "Atoms, catalogs & graduation",
          "is_current": true
        }
      ]
    },
    {
      "type": "heading",
      "text": "Atoms, catalogs & graduation"
    },
    {
      "type": "body",
      "text": "An **atom** is one typed UI contract: a `type` name, documented fields, and a renderer implementation per surface. The A2UI Catalog ships 468 stable atoms — from `paragraph` to `airspace_command_deck` — grouped into **packs** (charts, effects, presentation, competition…) so a host can enable exactly the vocabulary it wants."
    },
    {
      "type": "body",
      "text": "Discovery is machine-readable: the **ARD catalog** at `/.well-known/ai-catalog.json` lists every stable atom with its fields and surfaces, so an agent can learn the vocabulary without a human in the loop. Builder prompts are generated from the same schema — one source of truth, no drift."
    },
    {
      "type": "body",
      "text": "Atoms have a lifecycle. **`stage: preview`** atoms live in the repo only — no published page, no catalog entry, no schema promises; they may even be deliberately field-less. **Stable** atoms get an atom page, catalog entries, schema validation, and contract tests. Promotion is enforced by CI: publish artifacts must exactly equal the stable set."
    },
    {
      "type": "callout",
      "kind": "tip",
      "title": "A worked example — the rocket",
      "text": "gdm_rocket_panel shipped as hand-curated, field-less preview content inside the MCP Apps demo page. Once proven, it graduated: typed fields (side, layer, loop), a published page, catalog entries. A fireworks animation now holds the preview slot it came from. That doorway — curated first, schema when proven — is the catalog's intake pipeline."
    },
    {
      "type": "knowledge_check",
      "question": "Does a `stage: preview` atom appear in /.well-known/ai-catalog.json?",
      "options": [
        "Yes — everything in the repo is published",
        "Yes, but flagged as experimental",
        "No — only stable atoms are published; preview stays repo-only",
        "Only if it belongs to a pack"
      ],
      "correct": 2,
      "explanation": "Every published artifact (spec.json, atom pages, the ARD catalog, builder prompts) is generated from the stable set only — and tests fail the build if they ever diverge."
    },
    {
      "type": "key_takeaways",
      "points": [
        "Atom = typed contract + per-surface renderer; packs group the vocabulary",
        "Agents discover atoms via the ARD catalog — no human docs required",
        "preview → stable is a tested, enforced pipeline, not a convention",
        "Curated one-off content is the intake funnel for new atoms"
      ]
    },
    {
      "type": "lesson_nav",
      "module_label": "Learn A2UI + MCP",
      "current_title": "Atoms, catalogs & graduation",
      "prev_title": "The A2UI idea",
      "prev_url": "/s/learn-a2ui-a2ui-idea",
      "next_title": "Surfaces & the ?p= URL model",
      "next_url": "/s/learn-a2ui-surfaces-p-model"
    }
  ]
}
