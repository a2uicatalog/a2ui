{
  "title": "MCP Apps & controlled opaqueness",
  "theme": "dark",
  "blocks": [
    {
      "type": "breadcrumb",
      "items": [
        {
          "label": "Learn A2UI + MCP",
          "url": "/s/learn-a2ui"
        },
        {
          "label": "MCP Apps & controlled opaqueness",
          "is_current": true
        }
      ]
    },
    {
      "type": "heading",
      "text": "MCP Apps & controlled opaqueness"
    },
    {
      "type": "body",
      "text": "**MCP Apps** extends MCP so a tool result can carry an interactive UI. The server declares a UI resource; the host renders it in a **sandboxed iframe** (the *View*) and delivers data to it over a mediated postMessage channel. The model stays in the loop for tool calls — the View itself never talks to the network."
    },
    {
      "type": "steps",
      "items": [
        {
          "label": "1 · ui/initialize",
          "text": "The View posts ui/initialize to the Host as soon as it loads"
        },
        {
          "label": "2 · InitializeResult",
          "text": "The Host replies with McpUiInitializeResult — capabilities, display modes"
        },
        {
          "label": "3 · initialized",
          "text": "The View acknowledges with the ui/notifications/initialized notification"
        },
        {
          "label": "4 · tool-result",
          "text": "The Host delivers the payload via ui/notifications/tool-result — the View renders it"
        }
      ]
    },
    {
      "type": "body",
      "text": "**The sandbox is the point.** The iframe runs without `allow-same-origin`: the View genuinely cannot read or touch the host page's DOM, cookies, or storage. Every capability it has arrives through the declared message channel. For an A2UI View, the payload that arrives is declarative blocks — so even *inside* the sandbox, nothing executes that wasn't pre-vetted."
    },
    {
      "type": "body",
      "text": "**Controlled opaqueness:** because the View is opaque to the host but its *inputs* are mediated, curated one-off content and catalog-declarative atoms can ship side by side in one iframe — same channel, same sandbox, same audit story. That is what lets new components prove themselves in production before they graduate into the typed catalog."
    },
    {
      "type": "flashcard_deck",
      "accent": "#22d3ee",
      "label_front": "PROTOCOL",
      "label_back": "ANSWER",
      "cards": [
        {
          "front": "What delivers the payload to the View?",
          "back": "ui/notifications/tool-result, sent by the Host after the ui/initialize handshake completes."
        },
        {
          "front": "Why no allow-same-origin?",
          "back": "It severs the View from the host DOM entirely — the postMessage channel becomes the only doorway, and it's mediated."
        },
        {
          "front": "Where does A2UI fit?",
          "back": "A2UI payloads are the natural cargo: declarative blocks mean the View renders vetted components, not arbitrary code — defense in depth inside the sandbox."
        },
        {
          "front": "What is controlled opaqueness?",
          "back": "The host can't see into the View, but everything entering it is mediated — so curated and catalog content coexist safely, and proven pieces graduate into the schema."
        }
      ]
    },
    {
      "type": "key_takeaways",
      "points": [
        "MCP Apps = tool results that render UI in a host-sandboxed iframe",
        "Four-message handshake: ui/initialize → result → initialized → tool-result",
        "No allow-same-origin: the mediated channel is the View's only capability",
        "A2UI + MCP Apps compose: declarative cargo inside a mediated sandbox — audited at both layers"
      ]
    },
    {
      "type": "lesson_nav",
      "module_label": "Learn A2UI + MCP",
      "current_title": "MCP Apps & controlled opaqueness",
      "prev_title": "MCP fundamentals",
      "prev_url": "/s/learn-a2ui-mcp-fundamentals",
      "next_title": "Back to modules",
      "next_url": "/s/learn-a2ui"
    }
  ]
}
