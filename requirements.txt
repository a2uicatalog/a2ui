# Python build dependencies for the catalogue site build (Cloudflare Pages auto-installs
# this; keep in sync with the deps in .github/workflows/deploy.yml).
#   pyyaml   — schema/atom-packs parsing across the generators
#   markdown — renderers/web_article.py (imported by scripts/generate_atom_pages.py)
pyyaml
markdown
