# This repo's own real, empirically-confirmed test dependencies -- did not
# exist before this file (2026-08-21). Matches exactly what this repo's own
# CI (deploy.yml) already installs by hand; the full local test suite (540+
# tests) passes with just these three plus a `node` binary on PATH, which
# this file intentionally does not cover -- it is a separate runtime
# dependency (a real subprocess call, not a pip package) used by
# test_mcp_apps_bundle.py and 8 other files to exercise the compiled JS
# renderer bundle.
pytest
pyyaml
markdown
# Added 2026-08-24 for tests/a2ui_v1_conformance.py -- real jsonschema
# validation of emitted A2UI v1.0 messages against the vendored spec
# (tests/fixtures/a2ui_v1_0_spec/), replacing eyeballed field-name matching.
# MUST also be added to .github/workflows/deploy.yml's install step -- that
# workflow hardcodes its pip install list rather than reading this file (see
# this file's own header comment); a dependency added only here and not
# there is exactly the class of drift this estate hit twice on 2026-08-23
# (a deploy script's declared secrets silently diverging from what it
# actually installed/mounted).
jsonschema
# Added same day: stdlib `re` can't compile the \p{...} Unicode-property
# syntax the real spec's Extensions schema uses -- see
# tests/a2ui_v1_conformance.py's own top-of-file comment for exactly where
# this bit. Same two-places-must-match discipline as jsonschema above.
regex
